# 交接 — 0.7.0 / schema 1.3 / detailed 1

本輪修正及操作以 [電池 SOC 稽核](BATTERY_SOC_AUDIT.md) 為準；下方 0.6.0 及舊簡報保留其當時範圍，不含本次修正。

- 逐時換電 kWh 保留絕對需求。ServiceFleet 依實際容量／SOH／能量映射配對；窗口內不可行時維持排隊、原始未交付 kWh 與零交易收入。逐車 SOC 模式仍依實际出／入包淨能量計價。
- 可選 `BatterySlotConfig.chargeBands`、`BatterySpec.energySOC` 均向後相容；未提供時明示固定功率／線性假設。比例曲線數值可為 null 草稿，使用前驗證完整性。電池內部效率與上游轉換損耗分開。
- `battery-trace` 保留可壓縮的恆功率時段與換電事件身份，任意時間從能量反解 SOC。電池艙顯示狀態、SOC、庫存與補電功率；累計模式採終點 SOC。期末零長度結帳列不計新能量。
- 完整模型頁明確展開逐艙繼承值至草稿，再套用；保留既有覆寫。新建覆寫會固定單艙數值，後續站級變更需調整或停用覆寫。
- 設定 JSON／CSV、專案 JSON、結果 XLSX 完整往返；新增 Battery_SOC_Trace 與 Battery_SOC_Curves。全圖 JSON 包含游標各艙狀態。不同電池艙／槍不得共用服務 sink；電池端點不得由通用電纜／轉換模型覆蓋其充入效率。
- 網站新增三個各三日 SOC 固定案例与即時獨立比對。固定輸入須完整匹配，缺少時序或實際數值不符不能沿用通過結論。
- 新模型維持最長 1 分鐘內事件細分；SOC 分段／目標／交換／停機邊界精確切步。未知實測曲線不得宣稱已計入真實 BMS 特性，現場獲利仍須校準。

最終驗收：169項核心、37項正式Worker／SSR、1100項新SOC／SOH比較、47489項舊比較及33803項完整模型比較全部通過，型別與建置成功。正式Worker亦執行三個新SOC案例並與來源引擎完整結果及獨立預期比較。

驗證結果、第一次診斷回歸失敗與修正記錄見 [VERIFICATION.md](VERIFICATION.md)；執行證據見 `docs/evidence/v0.7.0/`。公開網站的持續發布授權有效。

---

# 交接 — 0.6.0 / schema 1.3 / detailed 1

本節為目前完整模型交接；下方 0.5.0 及更早內容保留歷史語義與證據，不代表新版仍缺少同一功能。操作以 [COMPLETE_MODEL_GUIDE.md](COMPLETE_MODEL_GUIDE.md)、[USER_GUIDE.md](USER_GUIDE.md) 為準。

- `Project.detailed` 明確選入新引擎；新完整預設及成功結果為 v0.6.0。未含 `detailed` 的歷史專案仍用原引擎，來源重播亦保留原表算術。不可改舊 V01–V03 fixture 迎合新模型。
- `detailed-model` 整合事件／物理／服務；`detailed-network` 以總負載評估非線性損耗及共享容量，補原圖 DD 外接槍、群組共享／互斥及受控方向母聯。預設 PCS 1600、每站 PCS 倉1–2／SST倉3–8、總24槍。
- `physical-models` 提供線纜 I²R／壓降、RC 熱、效率曲線、箱變空載／銅損、無功、設定式保護、PV／ESS／UPS／ATS；`service-fleet` 提供逐包初始容量／SOH／SOC、乘用車、逐車 profile／雙槍。未知必要值保持 `null`，物理 readiness gate 與財務完整性分開。
- `templates.ts` 與完整模型頁的「新增待填項目」建立欄位骨架／唯一 ID，不補容量或售價。新增先更新本頁草稿，再明確套用；新增端口才重建，重建前保存手改拓撲。無啟用開關的負載／事件／施工等缺值不能靠 `enabled=false` 規避。
- `flow-boundary` 使用同次快照，任意子分鐘窗口精算自放電及庫存；連續 `[from,to)`、儲能步末 spill `(from,to]`、換電期末關帳政策有明確揭露。容量 spill 是離散 kWh，不冒充瞬時 kW；全站消除儲能內部移轉，不重複計光伏、回充或售電。
- `extended-finance` 支援來源月帳單／15分鐘需量、周期加權存貨與內部轉撥消除、資產／租賃／OPEX、稅／應收／準備金及確認後的分月外推。部分月標示估計；缺資料的結論保持 `null`。物理施工事件與資產更換月仍須使用者一致設定。
- `detailed-profile` 交換完整設定 JSON／結構化 CSV；XLSX 追加模型、服務／储能／外售／電氣及財務帳。快照內容比對允許物件 key 順序不同，但不忽略數值、陣列次序或設定差異；編輯不能修改舊 `parameterSnapshot`。

## 最終發布驗證

已知獨立對照基準：新 V04–V06 為 **33,803 項**（50 項獨立數值／事件＋33,753 項逐帳對照），舊 V01–V03 為 **47,489 項**。兩組引擎範圍不同；不要直接把合計當成彼此獨立的全功能證明。

最終核心 134／134、正式 Worker／SSR 33／33、原獨立比較 47,489／47,489、新比較 33,803／33,803 全數通過；typecheck 與 Sites 正式建置退出碼均為 0。源檔 SHA256、日誌與簡報雜湊已封存於 [v0.6.0 證據](evidence/v0.6.0/release-checks.json)。詳細修正與邊界见 [驗證紀錄](VERIFICATION.md)。

對外報告：[PPTX](../public/reports/HighwaySwapSim-v0.6-Verification.pptx)、[PDF](../public/reports/HighwaySwapSim-v0.6-Verification.pdf)。舊簡報保留原版本，不拿來冒充 v0.6 全部驗收。

模型仍是準靜態、固定分支送端電壓、設定式故障／保護、一分鐘內事件切步與離散 ramp、啟發式 EMS。沒有全網潮流／電磁暫態／最優證明；周轉包初始 SOH 不是自動壽命模型；簡化財務不是所有供電／稅務契約。現場同步電表、SOC、交易、氣象、合約及成本的獨立校準尚須進行，不能宣稱真實獲利百分之百正確。

---

# 交接 — 0.5.0 / schema 1.3

SOC介面更新（2026-09-09）：設備與容量／站務配置新增每站百分比上下限、預覽及明確套用。packages/station-settings只原子更新該站SOC及全期換電需求；不重建拓撲、不改其他需求或結果。移除舊0–1直接編輯欄位；容量提示改讀目前SOC。0–1存檔欄位及引擎物理公式不變。操作與獨立三日驗算見SOC_SETTINGS.md，Master追加134章。

介面續更（2026-09-09）：移除「完整設備案例 · 513 kWh」按鈕，頂端僅留物理案例。首次專案採 physicalProjection(engineeringProject())，result 初始為 null，避免呈現原表重播成果。總覽保留設備圖、財務可先填成本、三例入口可執行；無成功結果時禁止 XLSX／加入比較。原 Worker 測算、成功後快照及舊結果保留行為不變；不在 SSR 同步執行模擬。

介面更新（2026-09-09）：依使用者要求移除上方「原始八月重播／舊模型」快捷按鈕與未使用匯入，操作手冊同步。此變更不改計算模型。

新增單台轉換效率與整套設備設定交換，操作與檔案契約見 [EQUIPMENT_SETTINGS.md](EQUIPMENT_SETTINGS.md)，Master Prompt 追加 133 章。

- `packages/equipment-efficiency` 集中解析 SST／箱變／PCS／DD 的 optional params.efficiency，缺省繼承全域；ASSEMBLY 才能覆寫。箱變輸出容量同步使用有效效率，保留共享輸入 kVA×PF 邊界；輸出額定設備不重乘自身效率。
- `packages/equipment-profile` 匯出／原子匯入整套設備 JSON（拓撲、效率、站務、工程、期別、排程）與設備參數 CSV。保留當前需求、價格、天數、到站方式與財務；CSV 保留原陣列順序。嚴格拒絕未知欄位、錯誤參數及不合法拓撲／排程；文字 CSV 具可逆公式保護。
- 供電設計新增設備選單、名稱及個別效率；效率頁列出所有轉換設備的繼承／覆寫及容量。未建模 metadata 單獨說明。總覽額定改讀實際節點；附件靜態容量表與全域比較明確標示不含個別覆寫／自訂拓撲。
- 52 核心／需求／設備測試 + 28 正式產物／SSR／物理測試，共 80 項全通過；三個原有多日案例 47,489 項独立比對全通過。新4日覆寫案例核對帳本、trace及XLSX；正式Worker覆寫結果與來源一致。
- 個別電池仍按每站同容量/SOC，乘用車仍只作交流負載；電纜長度/補償規格尚無實體損耗／無功模型。SOURCE_CHAIN 保留舊契約；來源 fixtures 與原 PPT 不改。
- 詳細驗證、首次測試輸入修正、限制见 VERIFICATION.md。既有公開工作網站與自動發布授權持續有效。

以下為 0.4.2 歷史交接：

# 交接 — 0.4.2 / schema 1.3

新增「逐時案例」需求 CSV／JSON 匯入匯出、每站每日直接編輯、到站方法／種子、來源價格欄位及 SOC 明確重算。需求格式版本 1，外部 day 從 1 開始；完整專案格式保持 1.3、內部 day 從 0 開始。

- `packages/service-profile` 共用既有 row schema，驗證完整 13 欄、唯一鍵、1–31 日、2 MiB／1,488 列及 20,000 請求上限。原子合併／全期取代只改 services，保留拓撲與既有列順序，避免種子事件變動。
- UI 獨立為 `service-panel.tsx`。檔案非同步讀取於卸載時失效，套用時使用目前專案；空白不靜默轉 0。來源總價不隨服務費改寫，SOC 衝突保留提示與來源原值。
- 操作文件見 [逐時需求](SERVICE_PROFILE.md) 與 [操作手冊](USER_GUIDE.md)，Master Prompt 追加 132 章。需求匯出為草稿，結果 XLSX 為最後成功快照；修改後須重算。
- 供電、庫存、事件生成與計價規則未改。需求檔不保存 arrival／seed／billing 或其他配置，完整重現須另存專案 JSON。仍無個別車輛指定秒數事件編輯器。
- 驗證成果及既有限制見 VERIFICATION.md。既有公開網站與自動發布授權持續有效；歷史證據及 0.3.0 PPT 保留原範圍。

以下為 0.4.1 歷史交接：

# 交接 — 0.4.1 / schema 1.3

文件補充：新增 [操作手冊](USER_GUIDE.md)，明列先選期別／天數／站務與設備配置、最後手動拓撲、JSON備份、重算、驗算、結果XLSX的顺序。補充自動重建、示範覆寫、匯入只恢復設計、全圖JSON不可直接當專案匯入、快照不含修改歷程。README及能量流文件已連結；本次只改文件，不改模型或介面。

新增「能量流驗算 → 區間累計 kWh」，按同一任意起訖積分全站、節點與連線；双端滑桿及精確分鐘、期初至目前／完整期間、區間峰值與原精度JSON同步。全站輸出採末端吸收，避免跨設備或跨站重複計量。Master Prompt追加131章，操作與數值契約見ENERGY_FLOW_UI.md。

本版不改供電分配、庫存與結算；結果積分實作在packages/power-trace，UI不另寫電量算式。舊結果無trace時不可假裝可用。65項測試全通過，47,489項独立解析比較全通過；TypeScript與正式建置通過。累計新回歸及最新整合結果見VERIFICATION.md。既有公開網站與自動發布授權不變。

以下為0.4.0歷史交接：

# 交接 — 0.4.0 / schema 1.3

本轮新增能量流驗算介面與逐事件功率記錄；完整規格與操作見 [ENERGY_FLOW_UI.md](ENERGY_FLOW_UI.md)，Master Prompt追加130章。原有分配、庫存與結算公式未改。

- 新頁有完整92/96連接圖、1–31日時間軸、kW/kWh切換、停機／聯鎖／受限狀態、節點守恆、相鄰追蹤與全圖JSON匯出。
- powerTrace為RunResult新增可選欄，結果在Worker記錄；逐訊號無損壓縮，不覆蓋來源fixtures或猜測瞬時功率。
- 三個固定多日案例重驗47,489項全通過。另有7項trace回歸及2項SSR完整圖驗證；最終60項測試全部通過，TypeScript與正式建置通過。詳細結果見VERIFICATION.md與evidence/v0.4.0。
- 3天完整物理案例一鍵載入，來源第一日需求重複3日，換電需求依SOC投影410.4 kWh。此為完整模型整合案例，不是第四個獨立解析案例。
- 舊PPT維持0.3.0封存；0.4.0驗證報告另存。既有公開網站與後續發布授權不變。

以下為0.3.0歷史交接：

# 交接 — 0.3.0 / schema 1.3

更新2026-09-08。指定repo與公開網站不變，既有發布授權保留於EXECUTION_CONTEXT.md。

- Master Prompt 原0–122章保留，追加123–129章，正式明定連續多日、元件帳、計價、獨立案例與驗收範圍。
- 本輪修正與證據以 VERIFICATION.md 為準。三例47,489項通過，金融／事件／電力缺口有回歸，首次不符與oracle更正均留存。
- Node24、Python3；執行 npm ci、npm run typecheck、npm test。npm run verify:scenarios 僅重跑數值；新結果寫入artifacts/verification，不覆蓋封存資料。
- 網站新增三例驗證頁、1–31日連續時序、來源電表及元件帳；原513kWh完整設備案例仍保留。
- 財務輸出使用result.parameterSnapshot，期末存貨不足不外推；收入減電費不稱會計淨利。IRR和金額有數值範圍與不可用狀態。
- 原始schema1.0/1.1/1.2匯入遷移到1.3，舊案例日數1；JSON是資料真相。XLSX依全期輸出、嚴格校驗ZIP與Unicode、上限64MiB；内嵌JSON上限8MiB。
- 本版仍不含實際AC潮流/保護配合、最優EMS、PV/ESS調度、電池效率曲線與老化、乘用車服務收入、完整稅／替換資產／應收款。工序與關鍵假設須用現場資料校準，有限測試不可推廣為真實獲利100%保證。
- docs/evidence/v0.3.0 是發布時封存；後續修改核心公式需新版本重驗，不能沿用舊PPT宣稱新版本已通過。

以下為0.2.0歷史交接（不是現行計算契約）：

# HighwaySwapSim 開發交接

更新：2026-09-08。產品 / engine 0.2.0，schema 1.2。指定 repo：pcpcchen-coder/HighwaySwapStationSim。

本輪需求與完整來源詳見 SUPPLEMENT_2026-09-08.md，已追加至 MASTER_PROMPT 第 115–122 章。原有 0–114 章及八月 CSV/JSON 保留。

## 本輪已實作

- 預設開啟 92 個設備、96 條連線的完整雙站案例。SST 每側兩個獨立工位，預留一台；16 個獨立 DD / 電池倉、2 堆、4 終端、8 支槍、2 重卡工位與 2 CATL 乘用車站。總覽具設備圖示與可點選明細，工程圖可縮放、捲動、拖曳、鍵盤移動、編輯參數與連線。
- 中壓進線 / 計量 / 出線 / 負荷開關、箱變、低壓饋線、補償櫃、PCS、雙母線、同類母線跨站直流共享。A 電網供兩側 SST；B 電網只供 B 箱變。跨站長度 150 m；額定 4000 kW 為可調假設。
- 重卡 513 kWh、SOC20–100%、每側8倉、DD560 kW。原始410 kWh資料重播與410.4 kWh物理投影分開載入；車次和充電來源不改。
- 架構2×1440 kW共享超充、4雙槍終端、600A/1000V槍；每支槍按車端電壓限流，共享終端、堆、PCS與箱變限額。單車槍ID在session中保持固定。
- 電池逐倉充電、PCS倉位分配、每側交流共用負載與SST運行負載。SOURCE_CHAIN限歷史簡化拓撲；詳細模型須使用ASSEMBLY。
- CATL官方99秒 /14–30倉/2.55–3.10m規格。附件500+50 kW另標示方案配置。乘用車只作共用交流負載、不計服務收入。
- 容量分析顯示source3370.76 vs derived3370.841889；1622.39及PCS餘量；同動率不能補足能量缺口；637.56V槍限382.536 kW。
- Schema1.0/1.1→1.2保留舊模型。XLSX詳細案例增加2個sheet，完整JSON還原；交易保留equipmentId。
- 保留0.1功能：48列原表、Web Worker、取消、快照比較、來源收入、效率對照、財務缺值防護、月度代表日爬坡、NPV/IRR/回收期、庫存能量差額。

## 實際簡化與下一步

- 供電仍為有向無環路徑分配，固定順序：共用AC負載、A倉/槍、B倉/槍。不求最優潮流，無功率動態、保護協調、短路或AC電網並聯。
- 每側1換電工位與7.5min是由8次/h導出；未按實際站房驗證。機械作業尚未因輔助用電短缺動態停機，停用工位會停止新作業。電池倉停用阻止補電，已存電池仍屬可用站內库存。
- DD/電池群數改動須同步拓撲；UI提供重建。手改圖面數量與station設定可不一致，缺失sink無供電；後續應提供完整一致性precheck。
- CATL選型與負載不是乘用車完整排隊模型；99s不推定持續吞吐。500+50kW、重卡200kW是配置上限，24h預設各0.5負載係數（可調）；設計容量表按滿載。沒有虛構乘用車收入。
- 跨站母線用來源bus→對側feed表示可用路徑，共享source限制；不代表真实同步／保護控制。線長已存但沒有電阻數據，I²R未啟用。
- ATS/UPS備援切換、DC/AC備援、ESS/PV/MPPT時序未實作；SST/UPS運行負載目前一個AC負載節點。功率補償櫃僅展示，kvar待確認。
- 直接充電仍為總能量需求與固定可調車端電壓，无SOC/溫度曲線；A12時2050kWh原值未確認。
- 容量worksheet以A配置對稱推算，是明示設計案例；手改非對稱拓撲時用24h受限結果評估。source全精度與圖示舍入值分欄。
- 金額仍為JS number，長期財務是代表日縮放，未含需量費/稅/替換/殘值完整資產模型。成本缺值不產生盈利結論。
- 沒有瀏覽器點擊/拖曳人工視覺測試；SSR產物測試不等同E2E。

## 執行

Node24。npm ci → npm run typecheck → npm test。變更物理模型需增加數值驗證。sim-kernel未因新增設備改動。範例快照位於 examples/supplement-*.json；實際驗證結果見 VERIFICATION.md。
