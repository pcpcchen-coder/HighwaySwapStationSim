# 雙側 DD 分配更新 — 2026-09-17

本次只更新完整物理預設：A、B 各由 PCS:SST＝2:6 改為 3:5。每站 DD 1–3 與電池艙 1–3 屬 PCS 組，DD 4–8 與電池艙 4–8 屬 SST 組。

- 每側仍 8 台 DD（560 kW/台）、8 艙；PCS/SST 共享組容量由 1120/3360 kW 改為 1680/2800 kW，這是 DD 數量加總，不提高上游供電容量。
- 依追加紅字將每側外接雙槍終端改為 PCS 1 台、SST 2 台，避免原先 ceil(DD數/2) 在奇數分配時意外新增終端與槍。新增可編輯欄位 detailed.topology.swapTerminalCounts；JSON/CSV/XLSX 均保留。省略欄位的歷史檔案仍依原重建規則。
- 共 142 節點、160 連線、16 個重卡艙及 20 槍（換電站外接 12 槍＋獨立超充 8 槍）。電網、箱變、SST、PCS 額定、獨立超充、乘用車、站務、SOC、效率、需求及財務設定保持不變；資產清單只移除對應刪除設備，其餘成本與合約欄位不改。
- 供電設計／能量流位置依實際 DD 饋線分組；總覽的 PCS/SST 文字也改讀實際連線。兩頁共用排列。來源電纜 ID 中的歷史文字不是供電判定依據，以 source/target 為準；重建時部分內部連線 ID 會更新。
- 修改後重新測算再匯出結果 XLSX。更新只作用於新載入的完整預設；既有 JSON 不會被默默升級。欲核對新預設，先保存現有草稿，再重新整理並載入「完整物理案例」。舊工程案例與獨立 fixture 均保留。

## 本次驗證

- 核心 175/175；含新增四項分配、功率算術與交換回歸，以及共用圖面座標／無重疊測試。
- 獨立小案例：16 艙各吸收 100 kW；每侧 PCS 饋線輸出 300/0.974 kW，SST 饋線輸出 500/0.974 kW。總購電功率＝1000/(0.974×0.98)+600/(0.974×0.98×0.98)，扣除 1600 kW 為損耗；7.5 分鐘電池吸收 200 kWh。
- 逐項比較更新前後完整預設，只有分配、對應 DD/艙連線、群組合計容量、使用者指定減少的 SST 雙槍終端/槍及對應資產清單改變；未採用新截圖其餘黑色額定數字。追加驗證每側 6 支外槍各 100 kW，兩側共 1200 kW；PCS/SST 每側饋線各 200/0.974、400/0.974 kW，已刪除槍位不可被調度。
- typecheck 與正式建置成功；正式 Worker/SSR 38/38 通過。
- 初次新測試發現舊版 feeder ID 是重接前的名稱，改以 source/target 核對電氣配置；XLSX 測試一開始誤用非同次重播快照，改用 result.parameterSnapshot，未改動數值期望來消除不符。
- 本次沒有修改 SOC 引擎或既有滿電浮點邊界問題，也不把配置測試宣稱為所有實際工況的保證。

## 充電樁追加變更

2026-09-17 的補圖覆蓋 9/16 初步「保留 1+3 終端」處理：現為每側 PCS 1 樁2槍＋SST 2樁4槍。兩側合計換電站外接 6 樁12槍；原有獨立超充堆、4樁8槍維持不變。末端節點、連線、服務槍位及資產清單同步移除每側最後一個 SST 終端和兩支槍，避免圖上移除但測算仍可使用。
