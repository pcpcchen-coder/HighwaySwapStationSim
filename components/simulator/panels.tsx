"use client";
import { useState } from 'react';
import { Area, AreaChart, Bar, CartesianGrid, ComposedChart, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Button } from '../ui/button';
import { Input } from '../ui/input';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '../ui/table';
import { NumberField, Choice, Stat, fmt } from './controls';
import { NetworkDiagram } from './topology';
import type { Project, RunResult, StationId } from '../../packages/contracts/index.ts';
import { compareEfficiency } from '../../packages/electrical-engine/index.ts';
import { breakEven, investmentCase } from '../../packages/economics-engine/index.ts';
import { equipmentRegistry } from '../../plugins/equipment/index.ts';
import { sourceFooter } from '../../packages/reference/index.ts';
type Props={project:Project;setProject:(p:Project)=>void;result:RunResult};
const chartStyle={fontSize:11,fill:'#6d8179'};
export function DailyChart({result}:{result:RunResult}){const data=Array.from({length:24},(_,hour)=>({hour:`${hour.toString().padStart(2,'0')}:00`,A:result.hours.find(h=>h.station==='A'&&h.hour===hour)!.deliveredKWh,B:result.hours.find(h=>h.station==='B'&&h.hour===hour)!.deliveredKWh,price:result.parameterSnapshot.services.find(r=>r.station==='A'&&r.hour===hour)!.gridPrice}));return <div className="chart"><ResponsiveContainer width="100%" height="100%" initialDimension={{width:800,height:245}}><ComposedChart data={data} margin={{top:15,right:5,bottom:0,left:5}}><CartesianGrid strokeDasharray="3 5" vertical={false} stroke="#e2eae5"/><XAxis dataKey="hour" tick={chartStyle} axisLine={false} tickLine={false} interval={3}/><YAxis tick={chartStyle} axisLine={false} tickLine={false} width={48}/><YAxis yAxisId="price" orientation="right" tick={chartStyle} axisLine={false} tickLine={false} domain={[0,1.5]} width={32}/><Tooltip formatter={(value:unknown,name:unknown)=>[fmt(Number(value),2),name==='A'?'A 區 kWh':name==='B'?'B 區 kWh':'購電 CNY/kWh']}/><Bar dataKey="A" fill="#137e6f" radius={[3,3,0,0]} maxBarSize={12}/><Bar dataKey="B" fill="#94c6b4" radius={[3,3,0,0]} maxBarSize={12}/><Line dataKey="price" yAxisId="price" type="stepAfter" stroke="#c69a43" strokeWidth={2} dot={false}/></ComposedChart></ResponsiveContainer></div>;}
export function Overview({project,result,onDesign}:{project:Project;result:RunResult;onDesign:()=>void}){const t=result.totals;return <><div className="hero"><div><p className="eyebrow accent">HIGHWAY ENERGY LAB / 雙站協同</p><h1>每一度電，<br/>都有清楚的去向。</h1><p className="hero-copy">連接供電配置、補能需求與營運收益。<br/>從來源案例出發，驗證你的下一個設計。</p><Button variant="outline" onClick={onDesign}>檢視供電設計 ↗</Button></div><div className="hero-summary"><div className="status-line"><span className="status-dot"/>八月基準案例<span className="mono">24 h</span></div><div className="hero-number">{fmt(t.deliveredKWh/1000,2)}<span>MWh</span></div><p>{result.mode==='SOURCE_REPLAY'?'來源逐列交付電量':'受限模擬實際交付電量'}</p><div className="mini-pair"><span>A / B 服務區</span><strong>{fmt(t.completed)} 次已完成</strong></div></div></div><div className="stats-grid"><Stat label="補能需求" value={fmt(t.requestedKWh)} unit="kWh" note="來源逐時合計，保留資料疑點"/><Stat label="交付完成率" value={fmt(t.requestedKWh?t.deliveredKWh/t.requestedKWh*100:0,1)} unit="%" note={result.mode==='SOURCE_REPLAY'?'重播值；尚非設備可行性驗證':`尚未交付 ${fmt(t.unservedKWh)} kWh`}/><Stat label="計費收入" value={fmt(t.revenue)} unit="CNY" note="能源費 + 服務費，尚未扣成本"/><Stat label={result.mode==='SOURCE_REPLAY'?'資料來源差額':'最大能量殘差'} value={result.mode==='SOURCE_REPLAY'?'2,050':fmt(t.maxBalanceResidual,6)} unit="kWh" note={result.mode==='SOURCE_REPLAY'?'A 區充電總計少列 12 時資料':'每小時輸入 − 輸出 − 損耗 − 庫存變化'}/></div><div className="overview-grid"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">DAILY ENERGY PROFILE</p><h2>需求與電價，放在同一條時間軸。</h2></div><div className="legend"><i/>A 區 <i className="light"/>B 區 <i className="gold"/>電價</div></div><DailyChart result={result}/></section><section className="panel source-card"><p className="eyebrow">DATA RECONCILIATION</p><h2>讓差異保持可見。</h2><p>來源原表與逐列計算並不完全相同。系統保留兩者，不為了配合總計改動單筆資料。</p><div className="reconcile-row"><span>A 區原圖充電</span><strong>35 次 / 12,430 kWh</strong></div><div className="reconcile-row"><span>A 區逐列重算</span><strong>36 次 / 14,480 kWh</strong></div><div className="notice">差額 = 12 時的 1 次 / 2,050 kWh</div></section></div><section className="panel network-overview"><div className="panel-heading"><div><p className="eyebrow">ELECTRICAL CONFIGURATION</p><h2>雙服務區供電概覽</h2></div><Button variant="ghost" onClick={onDesign}>開啟工程工作室 ↗</Button></div><NetworkDiagram project={project}/></section></>;}
export function ServicePanel({project,setProject}:{project:Project;setProject:(p:Project)=>void}){const [station,setStation]=useState<StationId>('A');const rows=project.services.filter(r=>r.station===station);const sum=(key:'swapCount'|'swapKWh'|'chargeCount'|'chargeKWh')=>rows.reduce((s,r)=>s+r[key],0);const fields=[['swapCount','換電次數'],['swapKWh','換電 kWh'],['chargeCount','充電次數'],['chargeKWh','充電 kWh'],['gridPrice','購電價'],['swapFee','換電服務費'],['chargeFee','充電服務費']] as const;return <section className="panel"><div className="panel-heading"><div><p className="eyebrow">24-HOUR SOURCE PROFILE</p><h2>逐時補能與定價</h2></div><Choice label="服務區" value={station} onChange={v=>setStation(v as StationId)} options={[["A","A 服務區"],["B","B 服務區"]]}/></div><p className="panel-note">金額單位 CNY/kWh。修改需求與費率後重新測算。來源顯示總價另行保留，精確分項模式使用購電價 + 服務費。</p><div className="stats-grid compact"><Stat label="換電" value={fmt(sum('swapCount'))} unit="次"/><Stat label="換電交付" value={fmt(sum('swapKWh'))} unit="kWh"/><Stat label="充電" value={fmt(sum('chargeCount'))} unit="次" note={`來源底部：${sourceFooter[station].charge} 次`}/><Stat label="充電交付" value={fmt(sum('chargeKWh'))} unit="kWh" note={`來源底部：${fmt(sourceFooter[station].chargeKWh)} kWh`}/></div><div className="table-scroll"><Table><TableHeader><TableRow><TableHead>時間</TableHead>{fields.map(([key,label])=><TableHead key={key}>{label}</TableHead>)}<TableHead>閒（原值）</TableHead></TableRow></TableHeader><TableBody>{rows.map(row=><TableRow key={row.hour} className={station==='A'&&row.hour===12?'source-conflict':''}><TableCell className="mono">{String(row.hour).padStart(2,'0')}:00</TableCell>{fields.map(([key,label])=><TableCell key={key}><Input aria-label={`${station} ${row.hour}時 ${label}`} type="number" min="0" step={key.endsWith('Count')?1:'any'} value={row[key]} onChange={e=>setProject({...project,services:project.services.map(r=>r.station===station&&r.hour===row.hour?{...r,[key]:Number(e.target.value)}:r)})}/></TableCell>)}<TableCell className="muted">{row.idleRaw??'未提供'}</TableCell></TableRow>)}</TableBody></Table></div></section>;}
export function EfficiencyPanel({project,setProject}:{project:Project;setProject:(p:Project)=>void}){const e=project.efficiency;const comparison=compareEfficiency(project);const {sst,pcs}=comparison;const refs=[['SST',98.5,98,98.12],['箱變',99,98.8,97.5],['PCS',98.6,98.3,98],['DD 模組',98.5,98,97.5],['電纜',99.9,99.9,99.9],['充電機（含 DD / 電纜）',98.4,97.9,97.4],['SST 全流程',96.93,95.94,95.57],['PCS 全流程',96.05,95.08,93.07]];return <div className="two-column"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">EFFICIENCY BOUNDARIES</p><h2>相同交付，兩條路徑。</h2></div></div><div className="efficiency-path"><span>10 kV</span><b>SST</b><span>→</span><b>充電機</b><strong>{fmt(sst*100,2)}%</strong></div><div className="efficiency-path secondary"><span>10 kV</span><b>箱變</b><span>→</span><b>PCS</b><span>→</span><b>充電機</b><strong>{fmt(pcs*100,2)}%</strong></div><div className="stats-grid compact two"><Stat label="效率差" value={fmt(comparison.percentagePoints,2)} unit="百分點"/><Stat label="固定交付購電節省" value={fmt(comparison.purchaseSavingPercent,3)} unit="%"/></div><p className="notice">充電機效率已包含 DD 與電纜；不能再乘一次。這是加權摘要比較，並非全年實測效率。</p><Table><TableHeader><TableRow><TableHead>來源項目</TableHead><TableHead>峰值</TableHead><TableHead>滿載</TableHead><TableHead>加權</TableHead></TableRow></TableHeader><TableBody>{refs.map(row=><TableRow key={row[0]}>{row.map((v,i)=><TableCell key={i}>{i===0?v:`${Number(v).toFixed(2)}%`}</TableCell>)}</TableRow>)}</TableBody></Table></section><aside className="panel inspector"><h2>效率模式與參數</h2><Choice label="計算邊界" value={e.mode} onChange={mode=>setProject({...project,efficiency:{...e,mode:mode as typeof e.mode}})} options={[["SOURCE_CHAIN","來源全流程摘要"],["ASSEMBLY","設備組裝乘積"]]}/>{(e.mode==='SOURCE_CHAIN'?(['sstSource','pcsSource'] as const):(['sst','transformer','pcs','charger'] as const)).map(key=><NumberField key={key} label={({sstSource:'SST 全流程',pcsSource:'PCS 全流程',sst:'SST',transformer:'箱變',pcs:'PCS',charger:'充電機'})[key]} value={e[key]*100} unit="%" min={1} max={100} onChange={value=>setProject({...project,efficiency:{...e,[key]:(value??1)/100}})}/>)}<hr/><h3>交付 10,000 kWh 的靜態換算</h3><p className="metric-line">SST 購電 <strong>{fmt(comparison.sstInputKWh,2)} kWh</strong></p><p className="metric-line">PCS 購電 <strong>{fmt(comparison.pcsInputKWh,2)} kWh</strong></p><p className="tiny muted">僅包含所選效率邊界，另計輔助負載與電池庫存；不能當作完整日電費。</p></aside></div>;}
export function StationPanel({project,setProject}:{project:Project;setProject:(p:Project)=>void}){const fields=[['batteries','站內電池數','顆'],['capacityKWh','電池額定容量','kWh'],['readySOC','Ready SOC','0–1'],['returnSOC','回收 SOC','0–1'],['bays','換電工位','位'],['swapMinutes','每次換電時間','min'],['guns','充電槍數','支'],['gunKW','單槍上限','kW'],['chargePoolKW','充電共享功率','kW'],['batteryChargeKW','每顆電池補電上限','kW'],['auxiliaryKW','固定輔助負載','kW']] as const;return <div className="two-column">{(['A','B'] as const).map(s=><section className="panel" key={s}><div className="panel-heading"><h2>{s} 服務區 · 營運配置</h2><span className="pill">DEMO ASSUMPTION</span></div><div className="form-grid">{fields.map(([key,label,unit])=><NumberField key={key} label={label} unit={unit} value={project.station[s][key]} onChange={value=>setProject({...project,station:{...project.station,[s]:{...project.station[s],[key]:value??0}}})}/>)}</div><p className="notice">電池、工位與作業時間為示範假設。換電 SOC 窗口須與每次交付需求對應；不相符的請求會保留待服務。</p></section>)}</div>;}
export function FinancePanel({project,setProject,result}:Props){const f=project.finance;const projection=investmentCase(project,result);const finance=projection?.projection;const reference=breakEven(4500,.3)!;return <><div className="two-column"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">INVESTMENT CASE</p><h2>先看營運，再看回收。</h2></div><span className="pill">CNY · 假設測算</span></div><p className="panel-note">成本與需求尚未經實地確認。來源 0.30 元服務費並不等於每度淨利；沒有成本資料時，不產生獲利結論。</p><div className="form-grid"><NumberField label="初始投資 CAPEX" unit="CNY" value={f.capex} onChange={capex=>setProject({...project,finance:{...f,capex}})}/><NumberField label="每日固定成本" unit="CNY/day" value={f.fixedDaily} onChange={fixedDaily=>setProject({...project,finance:{...f,fixedDaily}})}/><NumberField label="其他變動成本" unit="CNY/kWh" value={f.variablePerKWh} onChange={v=>setProject({...project,finance:{...f,variablePerKWh:v??0}})}/><NumberField label="年折現率" unit="%" max={100} value={f.discountRate*100} onChange={v=>setProject({...project,finance:{...f,discountRate:(v??0)/100}})}/><NumberField label="分析年期" unit="年" min={1} max={30} step={1} value={f.years} onChange={v=>setProject({...project,finance:{...f,years:v??10}})}/><NumberField label="車流爬坡期" unit="月" max={120} step={1} value={f.rampMonths} onChange={v=>setProject({...project,finance:{...f,rampMonths:v??0}})}/><NumberField label="初期需求比例" unit="%" max={100} value={f.startLoad*100} onChange={v=>setProject({...project,finance:{...f,startLoad:(v??0)/100}})}/><NumberField label="每年營運日數" unit="day" max={366} min={1} value={f.operatingDaysPerYear} onChange={v=>setProject({...project,finance:{...f,operatingDaysPerYear:v??365}})}/></div><Button variant="outline" onClick={()=>setProject({...project,finance:{...f,capex:6000000,fixedDaily:3000,variablePerKWh:.02}})}>載入財務示範值（非報價）</Button></section><section className="panel"><p className="eyebrow">REFERENCE THRESHOLDS</p><h2>59 次與 60 次，都保留算式。</h2><div className="stats-grid compact two"><Stat label="訪談售電量門檻" value={fmt(reference.energy)} unit="kWh/日"/><Stat label="換電次數換算" value={fmt(reference.swaps)} unit="次/日"/></div><div className="formula-card"><span>能量先增加 60% 再取整</span><strong>{reference.energyFirst} 次／日</strong><code>ceil(15000 × 1.6 ÷ 410)</code></div><div className="formula-card"><span>次數先取整再增加 60%</span><strong>{reference.integerFirst} 次／日</strong><code>ceil(37 × 1.6)</code></div><p className="tiny muted">4500 元／日只是為重現訪談門檻而設定的驗證 fixture，並非已確認成本。真正損益兩平需要成本與邊際貢獻。</p></section></div>{!projection?<div className="notice">請先在上方切換「受限模擬」並執行；原表重播只有收入，不提供物理購電成本或投資回收結論。</div>:!finance?<div className="notice">填寫 CAPEX 與每日固定成本，即可查看月度現金流與回收試算。</div>:<><div className="stats-grid"><Stat label="投資淨現值 NPV" value={fmt(finance.npv)} unit="CNY"/><Stat label="年化 IRR" value={finance.irr===null?'不適用':fmt(finance.irr*100,2)} unit={finance.irr===null?'':'%'} note={finance.irrStatus}/><Stat label="簡單回收期" value={finance.paybackMonths===null?'期內未回收':fmt(finance.paybackMonths/12,2)} unit={finance.paybackMonths===null?'':'年'}/><Stat label="最大累計資金缺口" value={fmt(finance.fundingGap)} unit="CNY"/></div><section className="panel"><div className="panel-heading"><h2>累計現金流</h2><span className="pill">逐月 · {f.years} 年</span></div><div className="chart"><ResponsiveContainer width="100%" height="100%" initialDimension={{width:800,height:245}}><AreaChart data={finance.rows}><CartesianGrid vertical={false} strokeDasharray="3 5"/><XAxis dataKey="month" tick={chartStyle}/><YAxis tick={chartStyle} width={70} tickFormatter={v=>`${fmt(Number(v)/10000)}萬`}/><Tooltip formatter={(v:unknown)=>[`${fmt(Number(v))} CNY`,'累計現金流']}/><Area type="monotone" dataKey="cumulative" stroke="#087f70" fill="#d4e9df" strokeWidth={2}/></AreaChart></ResponsiveContainer></div><p className="notice">代表日外推假設：按爬坡比例縮放每日收入與能量成本，固定成本照計。期末庫存缺口 {fmt(projection.replenishmentKWh,1)} kWh，以本日平均購價／效率估算另補 {fmt(projection.replenishmentCost,1)} 元；這不是完整年度 EMS 或替換資產／稅務模型。</p></section></>}</>;}
export function ResultsPanel({result}:{result:RunResult}){const physical=result.mode==='CONSTRAINED';return <section className="panel"><div className="panel-heading"><div><p className="eyebrow">CANONICAL RUN RESULT</p><h2>逐時結果與工程帳</h2></div><span className="pill">Engine {result.engineVersion}</span></div><div className="stats-grid compact"><Stat label="電網購入" value={physical?fmt(result.totals.gridKWh):'—'} unit="kWh"/><Stat label="路徑損耗" value={physical?fmt(result.totals.lossKWh):'—'} unit="kWh"/><Stat label="購電支出" value={physical?fmt(result.totals.gridCost):'—'} unit="CNY"/><Stat label="期末電池能源" value={physical?fmt(result.totals.finalStoredKWh):'—'} unit="kWh"/></div><DailyChart result={result}/><div className="table-scroll"><Table><TableHeader><TableRow>{['站','時','需求 kWh','交付 kWh','換 / 充次','購入 kWh','損耗 kWh','收入 CNY','電費 CNY','Ready','排隊','守恆殘差'].map(v=><TableHead key={v}>{v}</TableHead>)}</TableRow></TableHeader><TableBody>{[...result.hours].sort((a,b)=>a.hour-b.hour||a.station.localeCompare(b.station)).map(h=><TableRow key={`${h.station}${h.hour}`}><TableCell><span className={`station-tag ${h.station==='B'?'b':''}`}>{h.station}</span></TableCell><TableCell className="mono">{h.hour}:00</TableCell><TableCell>{fmt(h.requestedKWh)}</TableCell><TableCell>{fmt(h.deliveredKWh,1)}</TableCell><TableCell>{h.swapCount} / {h.chargeCount}</TableCell><TableCell>{physical?fmt(h.gridKWh,1):'—'}</TableCell><TableCell>{physical?fmt(h.lossKWh,1):'—'}</TableCell><TableCell>{fmt(h.revenue,2)}</TableCell><TableCell>{physical?fmt(h.gridCost,2):'—'}</TableCell><TableCell>{physical?h.ready:'—'}</TableCell><TableCell>{physical?h.queue:'—'}</TableCell><TableCell>{physical?h.balanceResidual.toExponential(1):'—'}</TableCell></TableRow>)}</TableBody></Table></div></section>;}
export function DeveloperPanel({result}:{result:RunResult}){return <div className="two-column"><section className="panel"><p className="eyebrow">PLUGIN REGISTRY</p><h2>讓模型可以繼續成長。</h2><Table><TableHeader><TableRow><TableHead>Type</TableHead><TableHead>設備</TableHead><TableHead>參數數</TableHead></TableRow></TableHeader><TableBody>{equipmentRegistry.list().map(p=><TableRow key={p.type}><TableCell className="mono">{p.type}</TableCell><TableCell>{p.label}</TableCell><TableCell>{p.parameters.length}</TableCell></TableRow>)}</TableBody></Table><p className="panel-note">時鐘、事件、亂數與 Registry 放在 sim-kernel。設備、電力、站務與財務公式獨立於畫面。</p></section><section className="panel"><p className="eyebrow">SCOPE & VALIDATION</p><h2>模型範圍</h2><ul className="scope-list"><li>24 小時來源重播與分鐘內事件切分。</li><li>有向無環供電路徑、共享容量與摘要效率。</li><li>換電電池庫存、工位排隊、充電共享功率。</li><li>Immediate / TOU 補電策略與固定種子到達。</li><li>月度爬坡財務試算與 JSON / 本系統 XLSX 交換。</li></ul><p className="notice">尚未支援完整雙母線並聯控制、保護暫態、3D 車流、PV/ESS 調度、MPC/RL、完整替換資產模型與通用 Excel 編輯後重播。完整需求保留於 repo。</p>{result.diagnostics.map((d,i)=><p className={`diagnostic ${d.severity}`} key={i}><code>{d.code}</code><span>{d.message}</span></p>)}<a className="repo-link" href="https://github.com/pcpcchen-coder/HighwaySwapStationSim/tree/main/docs" target="_blank" rel="noreferrer">開啟工程文件 ↗</a></section></div>;}
